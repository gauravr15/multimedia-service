spring.application.name=multimedia
spring.profiles.active=development,global
spring.cloud.config.label=1
spring.config.import=optional:configserver:http://localhost:8008/config-server

status.update.kafka.enabled=true

# Status catalog migrations are opt-in while the existing shared schema is inspected.
# Never auto-baseline an unknown populated schema.
# spring.flyway.enabled=${STATUS_CATALOG_FLYWAY_ENABLED:false}
# spring.flyway.locations=classpath:db/migration
# spring.flyway.baseline-on-migrate=false
# spring.flyway.validate-on-migrate=true

# # Catalog-neutral lifetime used by the new status domain (legacy Redis TTL is unchanged).
# status.duration-hours=${STATUS_DURATION_HOURS:24}
# status.max-duration-hours=${STATUS_MAX_DURATION_HOURS:168}

# # Transitional store for the new domain. Existing uploads do not use it in Phase 1.
# status.media.filesystem.root=${STATUS_MEDIA_FILESYSTEM_ROOT:${java.io.tmpdir}/odin-status-media}
# # This single-server environment currently permits the transitional filesystem store.
# # Set true only when a production-qualified shared store is configured; startup then fails closed otherwise.
# status.media.require-durable-storage=${STATUS_MEDIA_REQUIRE_DURABLE_STORAGE:false}

# # Phase 4 authoritative eligibility endpoint (profile.service.url supplies the service base URL).
# status.profile.eligibility-path=status/eligibility/batch

# status.lifecycle-reconciliation.enabled=${STATUS_LIFECYCLE_RECONCILIATION_ENABLED:true}
# status.lifecycle-reconciliation.interval-ms=${STATUS_LIFECYCLE_RECONCILIATION_INTERVAL_MS:60000}
# status.lifecycle-reconciliation.batch-size=${STATUS_LIFECYCLE_RECONCILIATION_BATCH_SIZE:100}
